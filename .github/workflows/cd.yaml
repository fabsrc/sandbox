name: Build & Deploy

on:
  push:
    branches:
      - "main"
    tags:
      - "v*"

jobs:
  deploy:
    name: Deploy 
    runs-on: ubuntu-latest
    strategy:
      matrix:
        env: [dev, stg, prd]
    environment: ${{ matrix.env }}
    steps:
      - env: 	
          ENV: ${{ matrix.env }}
        run: echo $ENV
